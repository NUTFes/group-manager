<%= simple_form_for @user_detail, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.hidden_field :user_id, value: @user_detail[:user_id] %>
  <%= f.input :name_ja,
    hint: t(".hint_name_ja")
    %>
  <%= f.input :name_en,
    hint: t(".hint_name_en")
  %>
  <%= f.association :department, label_method: :name_ja %>
  <%= f.association :grade, label_method: :name %>
  <%= f.input :tel,
    hint: t(".hint_tel")
  %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                welcome_index_path, :class => 'btn btn-default' %>
<% end %>
