<%= render 'dashboard' %>

<%# 副代表未登録の警告 %>
<%= render partial: 'shared/warning_subrep' %>
<%= render partial: 'shared/warning_place_order' %>
<%= render partial: 'shared/warning_stage_order' %>
<%= render partial: 'shared/warning_stage_common_option' %>


<%# Welcome Indexに表示する申請項目の表示を制御する %>
<% @config_panel.each do |panel| %>
  <%= show_enable_panel(panel, @num_nosubrep_groups, @group_count) %>
<% end %>
