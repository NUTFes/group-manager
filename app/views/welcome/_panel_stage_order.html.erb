<% if @is_stage_order_incomplete or @is_stage_common_option_empty %>
  <%# 未登録の団体がある場合には枠をwarningにする %>
  <div class="panel panel-warning">
<% else %>
  <div class="panel panel-primary">
<% end %>
  <div class="panel-heading">
    <h3 class="panel-title">ステージ利用</h3>
  </div>
  <div class="panel-body">
    ステージを利用する団体はこちらから申請して下さい。<br>
    対象: 参加形式が「ステージ企画」の団体
    <%= link_to t('welcome_controller.index'),
            stage_orders_path,
            :class => 'btn btn-default' %>
    <br>対象: 参加形式が「ステージ企画」の団体
    <%= link_to t('activerecord.models.stage_common_option'),
            stage_common_options_path,
            :class => 'btn btn-default' %>
  </div>
</div>
